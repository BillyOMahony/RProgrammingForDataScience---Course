
# Typing in a questionmark before a function brings up "help" on that funciton, arguements etc. 
?rnorm()

# Parameters can be specified as entered, therefore they don't have to be passed in sequence
rnorm(n=5, mean=10, sd=20)


?c()
# Other paramerters can be found with the ? 
?seq()
seq(from=10, to=15, length.out = 2)

?rep()

?sqrt()
